---
wps_hostname: localhost
# Configuration for Emu and Hummingbird WPS
wps_services:
  - name: emu
    hostname: "{{ wps_hostname }}"
    port: 5000
  - name: hummingbird
    hostname: "{{ wps_hostname }}"
    port: 5001
