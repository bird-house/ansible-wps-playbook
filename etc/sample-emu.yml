---
# Uncomment these options to run Emu through the local Slurm scheduler.
# slurm_enabled: true
# slurm_cpus: 4

wps_services:
  - name: emu
    hostname: localhost
    port: 5000
    # mode: scheduler
