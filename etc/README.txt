# Readme configs

Add your configs here.

Custom configs will be ignored by Git, like:

```
etc/custom-emu.yml
```

Use your config by linking it to `custom.yml`

```
$ ln -s etc/custom-emu.yml custom.yml
```
