---
- name: Stop nginx
  service:
    name: nginx
    state: stopped
  tags:
    - conf

- name: Get nginx configs
  find:
    path: "{{ nginx_conf_path }}"
    patterns: "*.conf"
  register: pywps_nginx_files
  tags:
    - conf

- name: Clean nginx configs
  file:
    path: "{{ item.path }}"
    state: absent
  with_items: "{{ pywps_nginx_files.files }}"
  tags:
    - conf

- name: Get supervisor configs
  find:
    path: "{{ supervisor_config_path }}/conf.d"
    patterns: "*.conf"
  register: pywps_supervisor_files
  tags:
    - conf

- name: Clean supervisor configs
  file:
    path: "{{ item.path }}"
    state: absent
  with_items: "{{ pywps_supervisor_files.files }}"
  tags:
    - conf

- name: Reload supervisor
  service:
    name: supervisord
    state: reloaded
  tags:
    - conf
