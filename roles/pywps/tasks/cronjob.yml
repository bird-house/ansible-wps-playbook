---
- name: Add MAILTO to PyWPS cron file
  community.general.cronvar:
    name: MAILTO
    value: "{{ cron_mailto }}"
    cron_file: ansible_pywps

- name: Clear PyWPS output directories
  ansible.builtin.cron:
    name: "clear wps outputs"
    minute: "3"
    hour: "*"
    user: "{{ cron_user }}"
    job: >-
      find {{ wps_basedir }}/outputs -mindepth 2 -maxdepth 2
      -ignore_readdir_race -type d
      -mmin +{{ wps_outputs_keep_minutes }} -regextype sed
      -regex ".*/[a-f0-9\-]\{36\}$"
      -prune -exec rm -rf -- {} \;
    cron_file: ansible_pywps
    disabled: "{{ cron_disabled }}"
    state: present

- name: Clear PyWPS status documents
  ansible.builtin.cron:
    name: "clear wps status"
    minute: "3"
    hour: "*"
    user: "{{ cron_user }}"
    job: >-
      find {{ wps_basedir }}/outputs -mindepth 2 -maxdepth 2
      -ignore_readdir_race -type f
      -mmin +{{ wps_outputs_keep_minutes }} -regextype sed
      -regex ".*/[a-f0-9\-]\{36\}\.xml$" -delete
    cron_file: ansible_pywps
    disabled: "{{ cron_disabled }}"
    state: present

- name: Clear PyWPS temporary process directories
  ansible.builtin.cron:
    name: "clear wps tempfiles"
    minute: "3"
    hour: "*"
    user: "{{ cron_user }}"
    job: >-
      find {{ wps_basedir }}/tmp -mindepth 2 -maxdepth 2
      -ignore_readdir_race -type d
      -mmin +{{ wps_temp_keep_minutes }} -name "pywps_process_*"
      -prune -exec rm -rf -- {} \;
    cron_file: ansible_pywps
    disabled: "{{ cron_disabled }}"
    state: present

- name: Ensure cron script dir exists
  ansible.builtin.file:
    path: "{{ cron_script_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Validate job-control settings
  ansible.builtin.assert:
    that:
      - wps_job_control_schedule | type_debug == 'dict'
      - wps_job_control_recovery_schedule | type_debug == 'dict'
      - wps_job_statistics_schedule | type_debug == 'dict'
      - wps_job_inspect_schedule | type_debug == 'dict'
      - wps_job_control_long_running_minutes | float > 0
      - wps_job_control_stale_after_minutes | float > 0
      - wps_job_control_database_stale_after_minutes | float > 0
      - wps_job_control_database_status_window_hours | float >= 3
      - wps_job_control_database_status_window_hours | float <= 24
      - >-
        (wps_job_control_long_running_minutes | float)
        < (wps_job_control_stale_after_minutes | float)
      - >-
        (wps_job_control_long_running_minutes | float)
        < (wps_job_control_database_stale_after_minutes | float)
      - wps_job_control_recovery_limit | int > 0
      - wps_job_incident_keep_days | int > 0
      - wps_job_incident_keep_minutes | int > 0
      - wps_job_incident_archive_dir | length > 0
      - >-
        not (wps_job_control_recovery_enabled | bool)
        or (wps_job_incident_archive_enabled | bool)
      - wps_missing_status_poll_window_minutes | float > 0
      - wps_missing_status_min_poll_count | int > 0
      - wps_missing_status_min_poll_duration_minutes | float >= 0
      - >-
        (wps_missing_status_min_poll_duration_minutes | float)
        < (wps_missing_status_poll_window_minutes | float)
      - wps_missing_status_recovery_limit | int > 0
      - >-
        not (cron_enabled | bool)
        or not (wps_job_control_monitor_enabled | bool)
        or (wps_job_control_stale_after_minutes | float) < (wps_outputs_keep_minutes | float)
    fail_msg: >-
      The job-control schedule must be a cron-field mapping, age must be
      positive and shorter than output retention for scheduled XML checks.
      The long-running warning must be positive and shorter than the stale
      threshold, the database status window must be between 3 and 24 hours,
      and recovery and incident-retention limits must be positive.
      Recovery requires incident archiving so the source XML and scheduler
      dump can be preserved before PyWPS updates them.
      Missing-status recovery also requires a positive polling window, poll
      count and recovery limit, and a minimum poll duration shorter than its
      polling window.

- name: Install PyWPS job-control script
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/scripts/pywps-job-control.py"
    dest: "{{ cron_script_dir }}/pywps-job-control.py"
    owner: root
    group: root
    mode: "0755"

- name: Install PyWPS database-monitor script
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/scripts/pywps-db-monitor.py"
    dest: "{{ cron_script_dir }}/pywps-db-monitor.py"
    owner: root
    group: root
    mode: "0755"

- name: Install PyWPS database-monitor command
  ansible.builtin.template:
    src: ./templates/db-monitor-helper.sh.j2
    dest: "{{ cron_script_dir }}/db-monitor"
    owner: root
    group: root
    mode: "0755"

- name: Install PyWPS XML inspection script
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/scripts/pywps-xml-inspect.py"
    dest: "{{ cron_script_dir }}/pywps-xml-inspect.py"
    owner: root
    group: root
    mode: "0755"

- name: Install PyWPS XML inspection command
  ansible.builtin.template:
    src: ./templates/xml-inspect-helper.sh.j2
    dest: "{{ cron_script_dir }}/inspect-jobs"
    owner: root
    group: root
    mode: "0755"

- name: Install PyWPS request-insights script
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/scripts/pywps-request-insights.py"
    dest: "{{ cron_script_dir }}/pywps-request-insights.py"
    owner: root
    group: root
    mode: "0755"

- name: Remove legacy PyWPS request-insights command
  ansible.builtin.file:
    path: "{{ cron_script_dir }}/request-insights"
    state: absent

- name: Install PyWPS insights command
  ansible.builtin.template:
    src: ./templates/request-insights-helper.sh.j2
    dest: "{{ cron_script_dir }}/insights"
    owner: root
    group: root
    mode: "0755"

- name: Create PyWPS job-incident archive base directory
  ansible.builtin.file:
    path: "{{ wps_job_incident_archive_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0750"
  when: wps_job_incident_archive_enabled | bool

- name: Create per-service PyWPS job-incident archive directories
  ansible.builtin.file:
    path: "{{ wps_job_incident_archive_dir }}/{{ item.name }}"
    state: directory
    owner: root
    group: root
    mode: "0750"
  loop: "{{ wps_services }}"
  when: wps_job_incident_archive_enabled | bool

- name: Clean up expired PyWPS job incidents
  ansible.builtin.cron:
    name: "clear archived PyWPS job incidents"
    minute: "3"
    hour: "*"
    user: "{{ cron_user }}"
    job: >-
      find {{ wps_job_incident_archive_dir }} -mindepth 2 -maxdepth 2
      -ignore_readdir_race -type f
      \( -name "*.xml" -o -name "*.dump" \)
      -mmin +{{ wps_job_incident_keep_minutes }} -delete
    cron_file: ansible_pywps
    disabled: >-
      {{ (cron_disabled | bool)
         or not (wps_job_incident_archive_enabled | bool) }}
    state: present

- name: Validate Slurm timeout and monitoring settings
  ansible.builtin.assert:
    that:
      - slurm_job_timeout_minutes | int > 0
      - >-
        slurm_job_timeout_minutes | int
        < wps_job_control_stale_after_minutes | float
      - >-
        slurm_job_timeout_minutes | int
        < wps_job_control_database_stale_after_minutes | float
      - slurm_job_monitor_schedule | type_debug == 'dict'
      - slurm_job_monitor_user | length > 0
      - slurm_job_monitor_long_running_minutes | int > 0
      - >-
        slurm_job_monitor_long_running_minutes | int
        < slurm_job_timeout_minutes | int
      - slurm_job_monitor_pending_critical | int > 0
    fail_msg: >-
      Slurm requires a positive runtime limit below the PyWPS stale threshold.
      Its optional monitor requires a cron-field mapping, a user, a positive
      pending-job critical threshold, and a positive long-running threshold
      below the hard runtime limit.
  when: slurm_enabled | bool

- name: Install Slurm job-monitor script
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/scripts/slurm-job-monitor.py"
    dest: "{{ slurm_job_monitor_script }}"
    owner: root
    group: root
    mode: "0755"
  when: slurm_enabled | bool

- name: Install interactive Slurm job-status script
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/scripts/slurm-job-status.py"
    dest: "{{ slurm_job_status_script }}"
    owner: root
    group: root
    mode: "0755"
  when: slurm_enabled | bool

- name: Remove legacy Slurm live-view shortcuts
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ cron_script_dir }}/slurm-top"
    - "{{ cron_script_dir }}/top"

- name: Install Slurm live-view shortcut
  ansible.builtin.template:
    src: ./templates/slurm-top.sh.j2
    dest: "{{ slurm_top_script }}"
    owner: root
    group: root
    mode: "0755"
  when: slurm_enabled | bool

- name: Remove obsolete Slurm job-control script
  ansible.builtin.file:
    path: /usr/local/sbin/slurm-job-control
    state: absent

- name: Remove Slurm job-monitor script when Slurm is disabled
  ansible.builtin.file:
    path: "{{ slurm_job_monitor_script }}"
    state: absent
  when: not (slurm_enabled | bool)

- name: Remove Slurm job-status tools when Slurm is disabled
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ slurm_job_status_script }}"
    - "{{ slurm_top_script }}"
  when: not (slurm_enabled | bool)

- name: Remove obsolete Slurm job-control cron entry
  ansible.builtin.cron:
    name: control overdue Slurm jobs
    user: root
    cron_file: ansible_slurm
    state: absent

- name: Add MAILTO to Slurm cron file
  community.general.cronvar:
    name: MAILTO
    value: "{{ cron_mailto }}"
    cron_file: ansible_slurm
    state: "{{ 'present' if slurm_enabled | bool else 'absent' }}"

- name: Schedule host-wide Slurm job monitoring
  ansible.builtin.cron:
    name: monitor Slurm jobs
    minute: "{{ slurm_job_monitor_schedule.minute | default('*/5') }}"
    hour: "{{ slurm_job_monitor_schedule.hour | default('*') }}"
    day: "{{ slurm_job_monitor_schedule.day | default('*') }}"
    month: "{{ slurm_job_monitor_schedule.month | default('*') }}"
    weekday: "{{ slurm_job_monitor_schedule.weekday | default('*') }}"
    user: root
    job: >-
      {{ slurm_job_monitor_script }}
      --user {{ slurm_job_monitor_user | quote }}
      --long-running-minutes {{ slurm_job_monitor_long_running_minutes }}
      --pending-critical {{ slurm_job_monitor_pending_critical }}
      --lock-file {{ slurm_job_monitor_lock_file | quote }}
      --log-file {{ slurm_job_monitor_log_file | quote }}
      --alert-file {{ slurm_job_monitor_alert_file | quote }}
    cron_file: ansible_slurm
    disabled: >-
      {{ (cron_disabled | bool) or not (slurm_job_monitor_enabled | bool) }}
    state: "{{ 'present' if slurm_enabled | bool else 'absent' }}"

- name: Install PyWPS job operator commands
  ansible.builtin.template:
    src: ./templates/job-control-helper.sh.j2
    dest: "{{ cron_script_dir }}/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - monitor
    - recover
    - statistics

- name: Remove obsolete recovery commands
  ansible.builtin.file:
    path: "{{ cron_script_dir }}/{{ item }}"
    state: absent
  loop:
    - recover-all
    - recover-polling
    - recover-stalled-jobs.py
    - recover-xml
    - recover-xml-db

- name: Remove obsolete lowercase PyWPS monitoring cron entries
  ansible.builtin.cron:
    name: "monitor stalled pywps jobs for {{ item.name }}"
    user: root
    cron_file: ansible_pywps
    state: absent
  loop: "{{ wps_services }}"

- name: Remove obsolete PyWPS recovery cron entries
  ansible.builtin.cron:
    name: "recover stalled pywps jobs for {{ item.name }}"
    user: root
    cron_file: ansible_pywps
    state: absent
  loop: "{{ wps_services }}"

- name: Remove obsolete shared PyWPS restart cron entry
  ansible.builtin.cron:
    name: restart pywps
    user: root
    cron_file: ansible_pywps
    state: absent

- name: Schedule PyWPS job monitoring
  ansible.builtin.cron:
    name: "monitor stalled PyWPS jobs for {{ item.name }}"
    minute: "{{ wps_job_control_schedule.minute | default('*/5') }}"
    hour: "{{ wps_job_control_schedule.hour | default('*') }}"
    day: "{{ wps_job_control_schedule.day | default('*') }}"
    month: "{{ wps_job_control_schedule.month | default('*') }}"
    weekday: "{{ wps_job_control_schedule.weekday | default('*') }}"
    user: root
    job: >-
      {{ conda_envs_dir }}/{{ item.name }}/bin/python
      {{ cron_script_dir }}/pywps-job-control.py
      --config /etc/pywps/{{ item.name }}.cfg monitor
    cron_file: ansible_pywps
    disabled: >-
      {{ cron_disabled }}
    state: present
  loop: "{{ wps_services }}"

- name: Schedule ordered PyWPS job recovery
  ansible.builtin.cron:
    name: "recover stalled PyWPS jobs for {{ item.name }}"
    minute: "{{ wps_job_control_recovery_schedule.minute | default('1-56/5') }}"
    hour: "{{ wps_job_control_recovery_schedule.hour | default('*') }}"
    day: "{{ wps_job_control_recovery_schedule.day | default('*') }}"
    month: "{{ wps_job_control_recovery_schedule.month | default('*') }}"
    weekday: "{{ wps_job_control_recovery_schedule.weekday | default('*') }}"
    user: root
    job: >-
      {{ conda_envs_dir }}/{{ item.name }}/bin/python
      {{ cron_script_dir }}/pywps-job-control.py
      --config /etc/pywps/{{ item.name }}.cfg recover
    cron_file: ansible_pywps
    disabled: >-
      {{ (cron_disabled | bool)
         or not (wps_job_control_recovery_enabled | bool) }}
    state: present
  loop: "{{ wps_services }}"

- name: Remove separate missing-status recovery schedule
  ansible.builtin.cron:
    name: "recover missing pywps status documents for {{ item.name }}"
    user: root
    cron_file: ansible_pywps
    state: absent
  loop: "{{ wps_services }}"

- name: Remove obsolete daily PyWPS statistics cron entries
  ansible.builtin.cron:
    name: "record daily PyWPS job statistics for {{ item.name }}"
    user: root
    cron_file: ansible_pywps
    state: absent
  loop: "{{ wps_services }}"

- name: Schedule hourly PyWPS job statistics
  ansible.builtin.cron:
    name: "record hourly PyWPS job statistics for {{ item.name }}"
    minute: "{{ wps_job_statistics_schedule.minute | default('4') }}"
    hour: "{{ wps_job_statistics_schedule.hour | default('*') }}"
    day: "{{ wps_job_statistics_schedule.day | default('*') }}"
    month: "{{ wps_job_statistics_schedule.month | default('*') }}"
    weekday: "{{ wps_job_statistics_schedule.weekday | default('*') }}"
    user: root
    job: >-
      {{ conda_envs_dir }}/{{ item.name }}/bin/python
      {{ cron_script_dir }}/pywps-job-control.py
      --config /etc/pywps/{{ item.name }}.cfg statistics
    cron_file: ansible_pywps
    disabled: "{{ cron_disabled }}"
    state: present
  loop: "{{ wps_services }}"

- name: Schedule hourly PyWPS XML request inspection
  ansible.builtin.cron:
    name: "record hourly PyWPS XML requests for {{ item.name }}"
    minute: "{{ wps_job_inspect_schedule.minute | default('2') }}"
    hour: "{{ wps_job_inspect_schedule.hour | default('*') }}"
    day: "{{ wps_job_inspect_schedule.day | default('*') }}"
    month: "{{ wps_job_inspect_schedule.month | default('*') }}"
    weekday: "{{ wps_job_inspect_schedule.weekday | default('*') }}"
    user: root
    job: >-
      /usr/bin/python3 {{ cron_script_dir }}/pywps-xml-inspect.py
      --config /etc/pywps/{{ item.name }}.cfg
      --service {{ item.name }}
      --state-file {{ cron_script_dir }}/.{{ item.name }}-xml-inspect-state.json
      --log-file {{ wps_log_dir }}/{{ item.name }}-requests.log
    cron_file: ansible_pywps
    disabled: "{{ (cron_disabled | bool) or not (wps_job_inspect_enabled | bool) }}"
    state: present
  loop: "{{ wps_services }}"

- name: Validate PyWPS restart schedule
  ansible.builtin.assert:
    that:
      - wps_restart_schedule in ['hourly', 'daily']
    fail_msg: wps_restart_schedule must be hourly or daily.

- name: Copy restart script
  ansible.builtin.template:
    src: ./templates/restart-pywps.sh.j2
    dest: "{{ cron_script_dir }}/restart-pywps.sh"
    owner: root
    group: root
    mode: "0744"

- name: Schedule PyWPS service restarts
  ansible.builtin.cron:
    name: "restart pywps {{ item.name }}"
    special_time: "{{ wps_restart_schedule }}"
    user: root
    job: >-
      {{ cron_script_dir }}/restart-pywps.sh {{ item.name }}
      > /var/log/pywps/restart-{{ item.name }}.log 2>&1
    cron_file: ansible_pywps
    disabled: >-
      {{ (cron_disabled | bool) or not (wps_restart_enabled | bool) }}
    state: present
  loop: "{{ wps_services }}"

- name: Copy smoke test script
  ansible.builtin.template:
    src: ./templates/smoke.sh.j2
    dest: "{{ cron_script_dir }}/smoke.sh"
    owner: root
    group: root
    mode: "0744"
