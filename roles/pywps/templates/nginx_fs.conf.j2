server {
    listen {{ fs_port }};
    server_name {{ fs_host | default('localhost') }};

    # better to redirect / to outputs
    location / {
      return 301 /outputs;
    }

    # PyWPS Outputs
    location /outputs
    {
        alias {{ wps_basedir }}/outputs;
        autoindex off;
    }

}
