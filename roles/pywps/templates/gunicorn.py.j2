bind = 'unix:///var/lib/pywps/{{ item.name }}.sock'
workers = 3
worker_class = 'sync'

# environment
raw_env = [
  "PYWPS_CFG=/etc/pywps/{{ item.name }}.cfg",
]

# logging
accesslog = '-'
errorlog = '-'
loglevel = 'INFO'
capture_output = True
