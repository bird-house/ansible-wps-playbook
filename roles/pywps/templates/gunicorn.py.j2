bind = 'unix:///var/run/pywps/{{ item.name }}.socket'
workers = 3
worker_class = 'sync'

# environment
raw_env = [
  "PYWPS_CFG=/etc/pywps/{{ item.name }}.cfg",
]

# logging
debug = True
errorlog = '-'
loglevel = 'INFO'
accesslog = '-'
