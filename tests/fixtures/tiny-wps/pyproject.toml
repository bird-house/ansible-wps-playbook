[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny-wps-fixture"
version = "0.1.0"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["tiny_wps"]
